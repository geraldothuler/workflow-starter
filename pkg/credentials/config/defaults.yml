# Default credential resolver configuration.
# Override per project: .workflow/credentials.yml
default_order:
  - env
  - keyring-macos
  - keyring-linux
  - encrypted-file
  - pass
  - 1password
  - aws-ssm
verbose: false
